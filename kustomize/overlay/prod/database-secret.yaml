apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: database-deployment
  name: database-deployment
spec:
  selector:
    matchLabels:
      app: database-pod
  template:
    metadata:
      labels:
        app: database-pod
    spec:
      containers:
      - name: mariadb
        env: 
        - name: "MYSQL_ROOT_PASSWORD"
          valueFrom:
            secretKeyRef:
              name: db-config
              key:  MYSQL_ROOT_PASSWORD
        - name: "MYSQL_DATABASE"
          valueFrom:
            secretKeyRef:
              name: db-config
              key:  MYSQL_DATABASE
